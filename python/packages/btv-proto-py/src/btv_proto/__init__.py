"""Stubs gRPC gerados — placeholder até a Fase 3.

Quando ativado: `just gen-proto` gera os stubs com betterproto a partir de
`platform/schemas/proto/{core,squad,llm}.proto`. Este pacote nunca é
editado à mão.
"""
