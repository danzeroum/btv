[project]
name = "forge-proto-py"
version = "0.1.0"
description = "Stubs gRPC gerados de schemas/proto — nunca editados à mão"
requires-python = ">=3.11"
dependencies = ["grpcio>=1.60"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/forge_proto"]
