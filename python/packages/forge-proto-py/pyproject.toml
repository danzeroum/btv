[project]
name = "forge-proto-py"
version = "0.1.0"
description = "Stubs gRPC gerados de platform/schemas/proto — nunca editados à mão"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/forge_proto"]
