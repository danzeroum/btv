[project]
name = "forge-eval"
version = "0.1.0"
description = "Avaliação contínua e A/B testing da plataforma Forge — implementação na Fase 6"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/forge_eval"]
