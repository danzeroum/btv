[project]
name = "btv-eval"
version = "0.1.0"
description = "Avaliação contínua e A/B testing da plataforma BuildToValue — implementação na Fase 6"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/btv_eval"]
