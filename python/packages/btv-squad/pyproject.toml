[project]
name = "btv-squad"
version = "0.1.0"
description = "Squad multi-agente da plataforma BuildToValue: orquestração, consenso, planejamento, HITL"
requires-python = ">=3.11"
# grpcio + btv-proto-py são exigidos pelo servidor SquadService e pelos
# clients gRPC (server.py/grpc_clients.py); o núcleo do squad (agentes,
# orquestrador, consenso) só precisa de pydantic.
dependencies = ["pydantic>=2", "grpcio>=1.60", "btv-proto-py"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/btv_squad"]

[tool.uv.sources]
btv-proto-py = { workspace = true }
