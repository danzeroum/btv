// Primitivas de UI do produto — feedback e estado assíncrono consistentes.
// Espelham as do console `web/` (primitives/), adaptadas aos tokens do btv-web.
export { AsyncStatus } from './AsyncStatus'
export { Modal, ConfirmModal } from './Modal'
export { ToastProvider, useToast } from './Toast'
